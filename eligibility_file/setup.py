from setuptools import setup, find_packages

setup(
   name='eligibility_file',
   version='1.0',
   description='Eligibility file processing',
   author='',
   author_email='',
   packages=find_packages(),
   install_requires=[],
)
